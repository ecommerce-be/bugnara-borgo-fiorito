package com.borghettofiorito.api.domain.enums;

/**
 * Reason why a visitor reached out through the contact form.
 * Helps the admins triage incoming messages.
 */
public enum ContactSubject {
    GENERIC_INFO,
    JOIN_INITIATIVE,
    SUGGEST_SPOT,
    OTHER
}
